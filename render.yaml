services:
  - type: web
    name: alltrails-scraper
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      playwright install
    startCommand: python app.py
    build:
      runtime: python-3.10
